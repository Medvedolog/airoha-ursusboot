# Environment policy for boards where the vendor owns the persistent env area.
# UrsusBoot must never save its own environment into vendor boot-area storage.

# CONFIG_ENV_IS_IN_UBI is not set
# CONFIG_ENV_REDUNDANT is not set
CONFIG_ENV_IS_NOWHERE=y
